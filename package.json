{
  "private": true,
  "description": "Policy Lens — Monorepo root. See admin-frontend/ and mobile/ for app packages.",
  "scripts": {
    "dev:admin": "cd admin-frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
